rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /subject/{subjectId} {
      allow read: if true;
      allow write: if request.auth.emailVerified == true;
    }
  }
}